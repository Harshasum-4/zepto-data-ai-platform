# Zepto Data & AI Platform

This repository contains three connected capstone modules. The current folder includes the completed `/data_pipeline` module. Run its setup commands from `data_pipeline/README.md`.
